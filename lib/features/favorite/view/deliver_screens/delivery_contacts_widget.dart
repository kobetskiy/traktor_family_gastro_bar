import 'package:flutter/material.dart';

class DeliveryContactsWidget extends StatelessWidget {
  const DeliveryContactsWidget({
    super.key
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('2'));
  }
}
