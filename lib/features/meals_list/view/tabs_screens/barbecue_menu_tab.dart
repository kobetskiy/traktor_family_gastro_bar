import 'package:flutter/material.dart';

class BarbecueMenuTab extends StatelessWidget {
  const BarbecueMenuTab({
    super.key
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('2'));
  }
}